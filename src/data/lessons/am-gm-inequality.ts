import type { Lesson } from "@/lib/types";

// KaTeX ルール: String.raw 内で $ の直後に { を置かない。必ず空白を挟む。
const r = String.raw;

export const AM_GM_INEQUALITY_LESSON: Lesson = {
  slug: "am-gm-inequality",
  title: "相加相乗平均の不等式",
  unit: "式と証明",
  summary:
    "2乗の非負性だけから導かれる最強ツール。「積が定数」と見抜いた瞬間、最小値が決まる。",
  tags: ["相加相乗平均", "最大最小", "不等式", "等号成立条件"],
  relatedProblemSlugs: ["am-gm-min-value"],
  practiceProblemSlug: "am-gm-min-value",

  // ── 概要（一覧カード＆ページ冒頭に表示）──────────────────────────────────
  content: r`## 相加相乗平均の不等式

2 つの非負の実数の算術平均（相加平均）は幾何平均（相乗平均）以上である。

$$\frac{a+b}{2} \geq \sqrt{ab} \qquad (a \geq 0,\ b \geq 0)$$

左辺が「足して 2 で割る」(**算術平均**)、右辺が「掛けて平方根」(**幾何平均**)。
この不等式は 2 乗の非負性という最も基本的な事実だけから導かれる。

> **この授業の目標：** 証明を暗記するのではなく、「なぜ成り立つか」を自分の言葉で語れるようになること。そして「積が定数」と見抜いた瞬間、最小値計算を自動的に終わらせる感覚を体得すること。`,

  // ── 【真理】厳密な証明 ─────────────────────────────────────────────────────
  proof: r`## 定理（相加相乗平均の不等式）

$a \geq 0,\ b \geq 0$ のとき、次の不等式が成り立つ。

$$\frac{a+b}{2} \geq \sqrt{ab}$$

等号は $a = b$ のときに限り成立する。

---

## 証明

$a \geq 0,\ b \geq 0$ のとき、$\sqrt{a}$ および $\sqrt{b}$ はそれぞれ実数として定義される。

**出発点：** 実数の 2 乗は常に 0 以上である（$\mathbb{R}$ の順序の基本性質）。

$$(\sqrt{a} - \sqrt{b})^2 \geq 0$$

左辺を展開すると

$$a - 2\sqrt{ab} + b \geq 0.$$

両辺に $2\sqrt{ab}$ を加えると

$$a + b \geq 2\sqrt{ab}.$$

両辺を正の数 $2$ で割ると

$$\boxed{\dfrac{a+b}{2} \geq \sqrt{ab}.} \qquad \square$$

**等号条件の精密な確認：**

$$\left(\sqrt{a} - \sqrt{b}\right)^2 = 0
\iff \sqrt{a} = \sqrt{b}
\iff a = b.$$

すなわち等号は $a = b$ のときに限り成立する。$\square$

---

## 証明の構造を俯瞰する

この証明が示しているのは、次の**3 段の等価変換**である。

$$\frac{a+b}{2} \geq \sqrt{ab}
\iff a + b \geq 2\sqrt{ab}
\iff a - 2\sqrt{ab} + b \geq 0
\iff \left(\sqrt{a}-\sqrt{b}\right)^2 \geq 0.$$

最後の式は「実数の 2 乗は 0 以上」という公理同然の事実。全ての矢印が双方向（$\iff$）なので、これが証明の完全な論理閉鎖になっている。

---

## 重要な変形型（正の数への制限）

$a > 0,\ b > 0$ のとき、$\sqrt{a} \to a,\ \sqrt{b} \to b$ と置き換えると

$$\frac{a^2 + b^2}{2} \geq ab \quad (a > 0,\ b > 0).$$

さらに $b = \dfrac{k}{a}\ (k > 0)$ と置くと積 $a \cdot \dfrac{k}{a} = k$ が定数になり

$$a + \frac{k}{a} \geq 2\sqrt{k} \quad (a > 0,\ k > 0).$$

**等号は $a = \sqrt{k}$ のとき。** これが実戦でもっとも頻繁に現れる形である。

---

## 一般化（$n$ 変数 AM-GM 不等式）

$a_1, a_2, \ldots, a_n \geq 0$ のとき

$$\frac{a_1 + a_2 + \cdots + a_n}{n} \geq \sqrt[n]{ {a_1 a_2 \cdots a_n} }.$$

$n = 2$ が本定理。$n \geq 3$ への拡張は数学的帰納法などで示せる（高校範囲外）。`,

  // ── 【極意】実戦での活かし方 ────────────────────────────────────────────────
  application: r`## 「積 = 定数」と見抜く技術

AM-GM を正しく引き抜けるかどうかは、**何と何の積が定数か**を一瞬で認識できるかにかかっている。

---

### 思考フロー Step 1 ── 「最小値問題」か確認する

AM-GM は $(\text{和}) \geq (\text{定数})$ の形の不等式なので、**和の最小値**を与える。
最大値問題には直接使えないことに注意。

---

### 思考フロー Step 2 ── 「掛けて定数」になるペアを探す

**例題：** $x > 0$ のとき $f(x) = x + \dfrac{9}{x}$ の最小値を求めよ。

まず 2 項の積を確認する。

$$x \cdot \frac{9}{x} = 9 \quad \text{（定数！）}$$

$x > 0$ より両項とも正。AM-GM を適用できる。

$$x + \frac{9}{x} \geq 2\sqrt{x \cdot \frac{9}{x}} = 2\sqrt{9} = 6.$$

---

### 思考フロー Step 3 ── 等号条件を「答え」に昇格させる

等号成立：$x = \dfrac{9}{x}$ すなわち $x^2 = 9$、$x > 0$ より $\mathbf{x = 3}$。

この $x = 3$ が制約 $x > 0$ を満たすことを確認。

$f(3) = 3 + 3 = 6$ で実際に最小値 $6$ が達成される。 $\therefore$ **最小値は $6$**。

> **重要：** 等号条件を確認しないまま「$\geq 6$ だから最小値は $6$」と書くのは**不完全な答案**。等号を達成する $x$ が存在することを示して初めて答えになる。

---

### 陥りがちな罠と対策

| 罠 | 症状 | 対策 |
|---|---|---|
| 積が定数でなく**和が定数** | $x + y = c$ のとき最大値問題 | 和一定 → 積の最大値（逆向き AM-GM） |
| 各項が非負でない | 負の項に AM-GM 不適用 | $a \geq 0,\ b \geq 0$ の前提を確認 |
| 等号条件が制約外 | $x = \sqrt{k}$ だが制約で不可 | 区間端での値を別途評価する |
| 3 項以上への拡張を間違える | $a+b+c \geq 3\sqrt[3]{ {abc} }$ の使い忘れ | $n$ 変数版 AM-GM を思い出す |

---

### 応用パターン速見表

| 式の形 | 積（定数） | 最小値 | 等号条件 |
|--------|-----------|--------|---------|
| $x + \dfrac{k}{x}$ &nbsp; $(k>0,\, x>0)$ | $k$ | $2\sqrt{k}$ | $x=\sqrt{k}$ |
| $ax + \dfrac{b}{x}$ &nbsp; $(a,b>0,\, x>0)$ | $ab$ | $2\sqrt{ab}$ | $x=\sqrt{b/a}$ |
| $x^2 + \dfrac{k}{x^2}$ &nbsp; $(k>0,\, x\ne 0)$ | $k$ | $2\sqrt{k}$ | $x^2=\sqrt{k}$ |
| $e^x + e^{-x}$ &nbsp; $(x\in\mathbb{R})$ | $e^x \cdot e^{-x}=1$ | $2$ | $x=0$ |

> **学習ポイント：** 「積が一定」と見抜けると、計算の見通しが立ちます。公式の形だけでなく、この**判断**を使えるように練習しましょう。`,
};
